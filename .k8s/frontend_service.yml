apiVersion: v1
kind: Service
metadata:
  name: coreflow-front-service

spec:
  type: NodePort

  selector:
    app: coreflow-front

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30000 